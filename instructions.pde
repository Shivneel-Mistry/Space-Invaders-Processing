void instructions() {
  background(0);
  textAlign(CENTER, CENTER);
  textSize(50);
  text("1. For a single player game, use mouse to move the spaceship and press the spacebar to shoot bullet.\nthe goal is to kill as many aliens as possible \n\n2.", width/2, height/3);
  text("Have fun!", width/2, 3*height/4);
  textSize(25);
  text("Press '0' to return to home page.", width/2, height-60);
 }
 
 
